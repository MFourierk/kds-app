# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist` est volontairement suivi par Git (pas dist-ssr, jamais utilisé ici)
# — pas de Node.js sur le VPS de prod, le build est fait en local avant
# chaque commit/push et déployé tel quel. Voir DEPLOY.md.
dist-ssr
dev-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
